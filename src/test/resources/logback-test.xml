<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property value="%d{yyyy-MM-dd HH:mm:ss.SSS} %X{email} %-40.40logger{39} : %m%n"/>

    <appender value="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
